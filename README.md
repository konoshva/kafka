# Спринт 2. Реализация потоковой обработки на базе Apache Kafka
## Класс MessageNetwork
Реализует систему обработки потоков сообщений с функциями блокировки пользователей и цензуры сообщений. 
Для каждого получателя может динамически настраиваться список запрещенных отправителей, что немедленно блокирует сообщения  
от них к этим получателям. Кроме того, также может динамически настраиваться общий список запрещенных слов, которые в этом 
случае сразу же маскируются в новых пользовательских сообщениях. 

## Приложение работает со следуещими топиками:

### blocked_users
Предназначен для управления списком заблокированных отправителей для определенных получателей 

`Key` - строка формата: `<Recipient>;<Sender>`

`Value` - строка с возможными значениями:
* `blocked` - канал связи заблокирован
* `<AnotherValue>` или tombstone - канал связи разблокирован
### blocked_words:
Предназначен для управления списком запрещенных слов

`Key` - строка формата: `<BlockedWord>`

`Value` - строка с возможными значениями:
* `blocked` - слово заблокировано
* `<AnotherValue>` или tombstone - слово разблокировано

### messages 
Dходные пользовательские сообщения

`Key` - строка формата: `<Recepient>;<Sender>`

`Value` - строка формата: `<Message>`

### filtered_messages 
сообщения после фильтрации

`Key` - строка формата: `<Recepient>;<Sender>`
`Value` - строка формата: `<Message>`

## Описание логики работы приложения
Последние настройки, заданные на уровне пар `<Recepient>;<Sender>` в топике `blocked_users`, материализуются в постоянное 
хранилище `blocked-users-store`. Аналогично, последние настройки, заданные для `<BlockedWord>` в топике `blocked_words`, 
материализуются в постоянное хранилище `blocked-words-store`. При появлении сообщения в топике `messages` оно после 
фильтрации и маскирования попадает в топик `filtered_messages`.

## Инструкция по запуску приложения
Перейдите в корневую папку и запустите команду разворачивания инфраструктуры:

    docker-compose up -d

С помощью [Kafka UI](http://localhost:8080/) убедитесь, что развернут кластер Kafka с одним брокером.
Выполните в другой консоли код для создани топика my_topic:
    
    docker exec -it kafka1-1 kafka-topics --create --topic my_topic --bootstrap-server kafka1:9092 --partitions 3 --replication-factor 2

При этом в первом окне завершится работа контейнера **kafka-topic_checker-1**, после чего будут запущены контейнеры:
* **kafka-producer-1** (завершится после отправки ста сообщений в топик my_topic)
* **kafka-batch_consumer-1**, **kafka-batch_consumer-2** (прочитает сообщения топика, но продолжит работать в режиме ожидания новых сообщений)
* **kafka-sm_consumer-1**, **kafka-sm_consumer-2** (прочитает сообщения топика, но продолжит работать в режиме ожидания новых сообщений)

Прочитать отправленные сообщения можно командой:

    docker logs kafka-producer-1

Здесь можно посмотреть, какие именно сообщения отправил producer.
Альтернативно, можно посмотреть лог контейнера в UI DockerDesktop.
Аналогичным образом, подставляя имена контейнеров получателей, можно посмотреть принятые ими сообщения. В kafka-batch_consumer-1
и kafka-batch_consumer-2 можно увидеть сообщения о пакетной фиксации смещений.

Список запущенных контейнеров (и их имена) можно получить командой:

    docker-compose ps

Еще один вариант контроля - проинспектировать Kafka кластер с помощью [Kafka UI](http://localhost:8080/):
* Перейти на вкладку **Topics** и убедиться, что топик my_topic содержит 100 сообщений
* Перейти на вкладку **Consumers** и убелиться, что обе консьюмер группы consumer-group1 (к ней принадлежат kafka-sm_consumer-\*)
  и consumer-group2 (к ней принадлежат kafka-batch_consumer-\*) не содержат не обработанных сообщений (значение в поле **Messages
  Behind** должно быть равно нулю)
* Кроме того здесь же можно увидеть все сообщения топика